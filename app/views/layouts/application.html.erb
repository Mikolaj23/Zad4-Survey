<!DOCTYPE html>
<html>
<head>
  <title>Survey3</title>
  <%= stylesheet_link_tag :all %>
  <%= javascript_include_tag :defaults %>
  <%= csrf_meta_tag %>
</head>
<body>
  <div id="container">
    <div id="user_nav">
      <% if user_signed_in? %>
	Zalogowany jako <%= link_to current_user.email, current_user %>  
        <%= link_to "Wyloguj", destroy_user_session_path %>
      <% else %>
	<%=  link_to "Zaloguj", new_user_session_path %>
      <% end %>
  </div>
  <hr>
<%= yield %>

</div>
</body>
</html>
